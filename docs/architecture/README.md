# docs/architecture/

Living descriptions of current-state design. One file per component.

Edit in place when the component changes. Delete when the component is removed — history belongs in `decisions/`.

Use `_template.md` to start a new architecture doc.
