# docs/decisions/

Immutable dated records of design choices. Format: `YYYY-MM-DD-short-title.md`.

Do not edit accepted decisions. If a decision is superseded, create a new decision and set `status: Superseded` on the old one with a link to the replacement.

Use `_template.md` to start a new decision.
