# docs/research/

Pre-decision explorations: tool evaluations, spikes, feasibility studies.

Lifecycle: `Active → Concluded` (produces a Decision) or `Active → Abandoned` (stopped, note why).

Move to `archive/` when concluded or abandoned. Update `status` in the frontmatter.

Use `_template.md` to start a new exploration.
